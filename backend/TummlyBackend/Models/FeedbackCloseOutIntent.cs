namespace TummlyBackend.Models
{
    /// <summary>
    /// Operator close-out CTA that ended follow-up without Feedback recovery.
    /// </summary>
    public enum FeedbackCloseOutIntent
    {
        MarkResolved = 0,

        MarkNoActionNeeded = 1,
    }
}
