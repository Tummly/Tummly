# Copy these into Railway → Service → Variables
# Railway uses double-underscore nesting for .NET configuration keys.

ASPNETCORE_ENVIRONMENT=Production

# Azure SQL or other hosted SQL Server (Railway does not provide SQL Server)
ConnectionStrings__DefaultConnection=Server=tcp:YOUR_SERVER.database.windows.net,1433;Initial Catalog=TummlyDB;User ID=YOUR_USER;Password=YOUR_PASSWORD;Encrypt=True;TrustServerCertificate=False;

# Run EF migrations on each deploy (recommended for QA)
Database__ApplyMigrationsOnStartup=true

# Frontend QA URL (Netlify or other)
Frontend__BaseUrl=https://your-qa-site.netlify.app

# Comma-separated list is not supported — use indexed keys for multiple origins
Cors__AllowedOrigins__0=https://your-qa-site.netlify.app
Cors__AllowedOrigins__1=http://localhost:5173

JwtSettings__Secret=replace-with-a-long-random-secret
JwtSettings__Issuer=TummlyAPI
JwtSettings__Audience=TummlyClient
JwtSettings__ExpiryMinutes=60

EmailSettings__SmtpServer=smtp.gmail.com
EmailSettings__Port=587
EmailSettings__SenderName=Tummly
EmailSettings__SenderEmail=your-email@gmail.com
EmailSettings__Username=your-email@gmail.com
EmailSettings__Password=your-gmail-app-password
