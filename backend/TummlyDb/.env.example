# Railway → TummlyDb service → Variables
# Name this service "TummlyDb" so the API can reference these values.

# Required. Must meet SQL Server password rules (8+ chars, upper, lower, number, symbol).
MSSQL_SA_PASSWORD=ChangeMe!Qa2026

# Optional. Created automatically by EF migrations from the API service.
# MSSQL_DATABASE=TummlyDB
